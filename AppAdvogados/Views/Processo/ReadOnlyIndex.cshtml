@model IEnumerable<AppAdvogados.Models.Processo>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Clientes</h2>

@if (!Model.Any())
{
    <p>Não temos nenhum cliente ainda.</p>
}
else
{
    <table id="processo" class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Data</th>
                <th>Causa</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var processo in Model)
            {
                <tr>
                    <td>@Html.ActionLink(@processo.Nome, "Edit", new { processo.Id })</td>
                    <td>@processo.Data</td>
                    <td>@processo.Causa</td>
                </tr>
            }
        </tbody>
    </table>
}